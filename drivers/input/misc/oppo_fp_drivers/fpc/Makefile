# VENDOR_EDIT
# FPC fingerprint driver Makefile - QCOM/SDM660 only

PROJECT_NAME = $(subst MSM_,,$(OPPO_TARGET_DEVICE))
DEFS = -DCONFIG_OPPO_FINGERPRINT_PROJCT=$(PROJECT_NAME)
ccflags-y += $(DEFS)

DEFS = -DCONFIG_OPPO_FINGERPRINT_KERNEL_VER=$(VERSION)
ccflags-y += $(DEFS)

obj-y  += fpc1020_tee.o
